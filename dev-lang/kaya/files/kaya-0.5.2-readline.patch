--- configure.ac.old	2011-09-30 13:59:34.000000000 -0400
+++ configure.ac	2011-09-30 14:00:38.000000000 -0400
@@ -523,8 +523,8 @@
 fi
 
 # and also in 6.10
-if [[ $ghcmajor -gt 6 ]] ||
-     ( [[ $ghcmajor -eq 6 ]] && [[ $ghcminor -ge 10 ]] ); then
+if ( [[ $ghcmajor -gt 6 ]] ||
+     ( [[ $ghcmajor -eq 6 ]] && [[ $ghcminor -ge 10 ]] ) ) && [[ $ghcmajor -lt 7 ]]; then
   readlinemodule='System.Console.Editline.Readline'
 else
   readlinemodule='System.Console.Readline'

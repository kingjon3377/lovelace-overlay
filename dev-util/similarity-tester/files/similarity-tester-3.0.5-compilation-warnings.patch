diff '--color=auto' -ru a/compare.c b/compare.c
--- a/compare.c	2024-02-08 06:04:53.000000000 -0500
+++ b/compare.c	2025-08-16 10:20:38.567329985 -0400
@@ -34,8 +34,8 @@
 		return (rg->rg_start <= i && i < rg->rg_limit);
 	} else {
 		/* looped-around range */
-		return (rg->rg_start <= i && i < end_of_text
-			|| beginning_of_text <= i && i < rg->rg_limit);
+		return (rg->rg_start <= i && i < end_of_text)
+			|| (beginning_of_text <= i && i < rg->rg_limit);
 	}
 }
 
diff '--color=auto' -ru a/debug.c b/debug.c
--- a/debug.c	2020-08-14 12:56:47.000000000 -0400
+++ b/debug.c	2025-08-16 10:21:26.309105288 -0400
@@ -3,6 +3,7 @@
 	$Id: debug.c,v 1.11 2020-08-14 16:56:47 dick Exp $
 */
 
+#include	<stdio.h>
 #include	<stdlib.h>
 #include	<unistd.h>
 #include	<ctype.h>
@@ -26,7 +27,7 @@
 
 static void
 wr_char(char ch) {
-	write(2, &ch, 1);
+	fputc(ch, stderr);
 }
 
 static void
diff '--color=auto' -ru a/options.c b/options.c
--- a/options.c	2024-02-08 06:04:53.000000000 -0500
+++ b/options.c	2025-08-16 10:21:56.856907939 -0400
@@ -130,6 +130,8 @@
 	case String_Type:
 		*(const char **)op->op_value = string;
 		break;
+	default:
+		break;
 	}
 
 	return consumed;
diff '--color=auto' -ru a/pass3.c b/pass3.c
--- a/pass3.c	2024-02-08 06:04:54.000000000 -0500
+++ b/pass3.c	2025-08-16 10:22:33.694944548 -0400
@@ -185,7 +185,7 @@
 	utf8_box u; clear_utf8_box(&u);
 
 	int len;
-	while (len = fill_ubox(f, &u)) {
+	while ((len = fill_ubox(f, &u))) {
 		/* take a critical look at what we've got */
 		char u0 = u.text[0];
 		if (u0 == '\n') break;			/* stop on end of line*/
diff '--color=auto' -ru a/textlang.l b/textlang.l
--- a/textlang.l	2024-02-08 06:04:54.000000000 -0500
+++ b/textlang.l	2025-08-16 10:23:01.812656893 -0400
@@ -18,6 +18,7 @@
 
 #include	"lex.h"
 #include	"lang.h"
+#include	"options.h"
 
 /* General language front end data */
 Token lex_token;
diff '--color=auto' -ru a/any_int.c b/any_int.c
--- a/any_int.c	2023-05-17 11:42:49.000000000 -0400
+++ b/any_int.c	2025-08-16 10:25:20.617819445 -0400
@@ -66,7 +66,7 @@
 	if (val <= 999) {
 		/* 'val' fits in " 999  ", which is a differnt format */
 		/* no rounding needed */
-		sprintf(res, " %3d  ", val);
+		sprintf(res, " %3lld  ", val);
 		return res;
 	}
 
